Place modded voice line files here. Supported formats: OGG, FLAC, Opus, MP3, WAV.

Files:
  vs.ogg       — VS screen announcer (replaces adx_VS)
  emsel.ogg    — Character select music (replaces adx_EmSel)

Enable modded voice lines in the F3 Mods menu.
