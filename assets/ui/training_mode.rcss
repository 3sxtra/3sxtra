/* ============================================================ *
 *  training_mode.rcss  —  Training Mode selector (overrides)    *
 *  Inherits: base.rcss → menu_shared.rcss → this               *
 * ============================================================ */

/* Panel + title + item styling inherited from menu_shared.rcss.
 * Add per-menu overrides below as needed. */
#tr-mode-panel { }
